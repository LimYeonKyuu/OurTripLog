import { createTheme } from '@mui/material/styles';

const theme = createTheme({
    palette: {
        primary: { main: '#3D74B6' },
        secondary: { main: '#FBF5DE' },
        tertiary: { main: '#EAC8A6' },
        quaternary: { main: '#DC3C22' },
    },
});

export default theme;